# Finance

Finance 是一个免安装的个人财务管理程序。

## 使用方法

1. 下载 `Finance.exe`。
2. 将程序放在具有写入权限的普通文件夹中。
3. 双击运行，首次启动会自动生成用户配置。

## 本次更新

- 新增 GitHub 静默检查更新。
- 无网络或 GitHub 访问失败时不影响程序启动。
- 更新文件发布到固定的 GitHub Release。

> 发布新程序前，请同步修改“本次更新”内容。
